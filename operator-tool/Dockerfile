FROM node:22-alpine

WORKDIR /app

COPY package.json ./
COPY server.mjs ./
COPY worker.mjs ./
COPY public ./public

RUN mkdir -p /app/data && chown -R node:node /app

USER node

ENV NODE_ENV=production
ENV HOST=0.0.0.0
ENV PORT=4173
ENV DATA_DIR=/app/data

EXPOSE 4173

CMD ["node", "server.mjs"]
